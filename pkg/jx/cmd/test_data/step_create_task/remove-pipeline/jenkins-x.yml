pipelineConfig:
  env:
    - name: FRUIT
      value: BANANA
    - name: GIT_AUTHOR_NAME
      value: PIDJENKINSKSBOTT
  pipelines:
    overrides:
      # Remove the pullRequest pipeline
      - pipeline: pullRequest

