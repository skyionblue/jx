pipelineConfig:
  env:
    - name: FRUIT
      value: BANANA
    - name: GIT_AUTHOR_NAME
      value: PIDJENKINSKSBOTT
  pipelines:
    overrides:
      # Remove the build lifecycle/stage
      - pipeline: release
        stage: build

